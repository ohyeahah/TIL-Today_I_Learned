# Kali_Linux


* Day 1 - 2025.10.18
  * check cpu(list cpu)
     - lscpu : list cpu
  * check memory, check list block, check ip address
    - free -h : check memory
    - lsblk : check list block
    - ip a: check IP address
  * test network connectivity in Linux
    - ping: send a signal
    - ping -c (number): ping count number, it repeat as number
  * test network connectivity in Window
    - ping -n (number): it repeat as number
      
 ---
 * Day 2 - 2025.12.05
   * directory
     - cd : change directory
   * file
     - vi (file name): made note in file name
     - cat (file name): show text in file
     - cp (file name) (location/new name): copy file
     - cp -r (source folder) (destination folder)
     - rm (file name): remove file
     - -r : Recursive
     - rmdir (directory name): remove directory
     - mkdir (new name): make directory
     - mv (file name) (location/new name): move file
     - mv (old name)(new name): changing name

